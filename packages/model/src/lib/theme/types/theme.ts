import type { ValueOf } from '../../../types/common/common';
import { Disign, Theme } from '../const/theme';

export type ThemeTypes = ValueOf<typeof Theme>;
export type DisignTypes = ValueOf<typeof Disign>;
